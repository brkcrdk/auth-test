interface AuthTokens {
  accessToken: string;
  refreshToken: string;
}

const authTestConfig: AuthTokens = {
  accessToken: "accessToken",
  refreshToken: "refreshToken",
};

export default authTestConfig;